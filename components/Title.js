const Title = ({ title }) => {
  return (
    <div className="flex items-center justify-center text-[40px] font-medium   sm:mt-[2.5rem] -mt-6  ">
      {title}
    </div>
  );
};

export default Title;
